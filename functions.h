#ifndef FUNCTIONS_H
#define FUNCTIONS_H

void updateScore(bool invert);
void checkWinCondition();
void callBallPosX(float speed);
void callBallPosY(float speed);
void collisionlogic(std::string player);

#endif
